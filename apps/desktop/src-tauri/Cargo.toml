[package]
name = "truths_forge_ai_desktop"
version = "0.1.0"
description = "Truth's Forge AI desktop wrapper"
authors = ["Truth's Forge"]
edition = "2021"

[lib]
name = "truths_forge_ai_desktop_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
